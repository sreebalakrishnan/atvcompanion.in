# -----------------------------------------------------------------------
# RETIRED — May 2026
# Site has moved from Static.app (atv-companion.sreebalakrishnan.in)
# to Hostinger (atvcompanion.in). Hostinger pulls directly from the
# main branch on push — no GitHub Actions workflow needed.
# Kept here for reference in case we need to roll back or repurpose.
# -----------------------------------------------------------------------

# name: Deploy to Static.app
#
# on:
#   push:
#     branches: [ main ]
#
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#
#       - name: Stage files for upload
#         run: |
#           mkdir -p staging
#           rsync -av \
#             --exclude='.git' \
#             --exclude='.github' \
#             --exclude='staging' \
#             --exclude='_root' \
#             --exclude='astrology-101-knowledge-base' \
#             --exclude='scripts' \
#             --exclude='tools' \
#             --exclude='*.sh' \
#             --exclude='*.bak' \
#             --exclude='*.bak2' \
#             --exclude='*.bak.*' \
#             --exclude='.gitignore' \
#             --exclude='.gitattributes' \
#             --exclude='.DS_Store' \
#             --exclude='**/.DS_Store' \
#             --exclude='README.md' \
#             --exclude='USER_TESTING.txt' \
#             --exclude='workbook-avatar-template.md' \
#             ./ staging/
#
#       - name: Deploy to Static.app
#         uses: designmodo/static-app-deploy-action@v1.0.0
#         with:
#           api-key: ${{ secrets.STATIC_APP_API_KEY }}
#           pid: 'rujcfa7h6w'
#           build-dir: 'staging'
